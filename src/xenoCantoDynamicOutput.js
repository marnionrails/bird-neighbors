const filterXenoCantoResponse = (response) => {
  let bestRecordingsArray = [];
  response.recordings.forEach((recording) => {
    if (recording.q === "A")
 })
}

export const dynamicResponseOutput = () => {

// }

// export const dynamicErrorOutput = () => {

// }